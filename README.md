# Google Maps in Angular

Setting up the project

1. Clone the project
2. Install dependencies:
   `npm i`
3. Generate the .env file based on env.template file
   - You can copy the content of env.template to .env file manually or run the command:
     `npm run set-envs`
4. Run the application with: `npm start`
